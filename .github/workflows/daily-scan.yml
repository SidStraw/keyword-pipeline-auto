name: Daily Keyword Scan

on:
  schedule:
    # Run daily at 3:17 AM UTC (避開午夜高峰時段，使用隨機分鐘數分散負載)
    - cron: '17 3 * * *'
  workflow_dispatch:
    # Allow manual triggering

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install GitHub Copilot CLI
        run: npm install -g @github/copilot

      - name: Install dependencies
        run: npm ci

      - name: Run keyword discovery script
        env:
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          GAS_WEB_APP_URL: ${{ secrets.GAS_WEB_APP_URL }}
          MY_CUSTOM_API_KEY: ${{ secrets.MY_CUSTOM_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          COPILOT_MODEL: ${{ secrets.COPILOT_MODEL }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SEED_KEYWORDS: ${{ secrets.SEED_KEYWORDS }}
        run: npx ts-node src/index.ts

# Required GitHub Secrets:
# - SERPER_API_KEY: Your Serper.dev API key for SERP data
# - GAS_WEB_APP_URL: The endpoint for the Google Apps Script Web App
# - MY_CUSTOM_API_KEY: For authenticating the POST request to GAS
# - GH_TOKEN: GitHub PAT with "Copilot Requests" permission for AI generation
#
# Optional GitHub Secrets:
# - SEED_KEYWORDS: Comma-separated seed keywords (if not provided, Copilot will generate them)
# - COPILOT_MODEL: AI model to use (default: claude-haiku-4.5, options: claude-sonnet-4.5, gpt-5)
# - DISCORD_WEBHOOK_URL: Discord webhook URL for pipeline notifications
